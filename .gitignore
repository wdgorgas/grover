# ── Secrets & runtime data — NEVER commit ──────────────────────
# v1's database and secret vault (contains a real API key)
archive/grover_v1/data/
archive/grover_v1/vault/
secrets.json
*.env
*.db
*.db-shm
*.db-wal

# ── Local tooling / machine-specific ────────────────────────────
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
node_modules/
